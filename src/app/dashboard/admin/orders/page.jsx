"use client";
import RoleRoute from "@/components/shared/RoleRoute";
import { getAdminOrdersApi, updateOrderStatusApi } from "@/lib/ordersApi";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { useState } from "react";
import toast from "react-hot-toast";
const orderStatuses = ["pending", "accepted", "processing", "shipped", "delivered", "cancelled", "rejected"];
export default function AdminOrdersPage() { const [search, setSearch] = useState(""); const queryClient = useQueryClient(); const { data, isLoading } = useQuery({ queryKey: ["admin-orders", search], queryFn: () => getAdminOrdersApi({ search, limit: 50 }) }); const orders = data?.orders || []; const updateMutation = useMutation({ mutationFn: updateOrderStatusApi, onSuccess: data => { toast.success(data.message || "Order updated."); queryClient.invalidateQueries({ queryKey: ["admin-orders"] }); }, onError: error => toast.error(error?.response?.data?.message || error.message) }); return <RoleRoute allowedRoles={["admin"]}><div className="space-y-6"><div><p className="text-sm font-bold uppercase tracking-[0.25em] text-orange-500">Admin Orders</p><h1 className="mt-2 text-3xl font-black text-slate-950 dark:text-white">Monitor marketplace orders</h1></div><div className="dashboard-card"><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Search product, buyer, seller..." className="mb-5 w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 dark:border-slate-700 dark:bg-slate-950 md:max-w-md"/><div className="overflow-x-auto">{isLoading ? <p className="font-semibold text-slate-500">Loading orders...</p> : orders.length === 0 ? <p className="font-semibold text-slate-500">No orders found.</p> : <table className="w-full min-w-[1000px] text-left"><thead><tr className="border-b border-slate-200 text-sm text-slate-500 dark:border-slate-800"><th className="py-3">Product</th><th className="py-3">Buyer</th><th className="py-3">Seller</th><th className="py-3">Payment</th><th className="py-3">Amount</th><th className="py-3">Status</th></tr></thead><tbody>{orders.map(order => <tr key={order._id} className="border-b border-slate-100 dark:border-slate-800"><td className="py-4 font-bold">{order.productTitle}</td><td className="py-4">{order.buyerInfo?.email}</td><td className="py-4">{order.sellerInfo?.email}</td><td className="py-4 capitalize">{order.paymentStatus}</td><td className="py-4 font-bold">৳{Number(order.totalAmount).toLocaleString()}</td><td className="py-4"><select value={order.orderStatus} onChange={event => updateMutation.mutate({ id: order._id, orderStatus: event.target.value })} className="rounded-2xl border border-slate-200 bg-white px-4 py-2 text-sm dark:border-slate-700 dark:bg-slate-950">{orderStatuses.map(status => <option key={status} value={status}>{status}</option>)}</select></td></tr>)}</tbody></table>}</div></div></div></RoleRoute>; }
