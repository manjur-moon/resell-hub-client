"use client";
import SimpleLineChart from "@/components/charts/SimpleLineChart";
import StatCard from "@/components/dashboard/StatCard";
import RoleRoute from "@/components/shared/RoleRoute";
import { getAdminDashboardApi } from "@/lib/statsApi";
import { useQuery } from "@tanstack/react-query";
import { PackageSearch, ReceiptText, Users, Wallet } from "lucide-react";
export default function AdminDashboardPage() { const { data } = useQuery({ queryKey: ["admin-dashboard"], queryFn: getAdminDashboardApi }); const stats = data?.stats || {}; const chartData = data?.charts?.userGrowth || []; return <RoleRoute allowedRoles={["admin"]}><div className="space-y-6"><div><p className="text-sm font-bold uppercase tracking-[0.25em] text-orange-500">Admin Dashboard</p><h1 className="mt-2 text-3xl font-black text-slate-950 dark:text-white">Platform control center</h1></div><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-4"><StatCard title="Total Users" value={stats.totalUsers || 0} description="Registered users" icon={Users}/><StatCard title="Total Products" value={stats.totalProducts || 0} description="Marketplace listings" icon={PackageSearch}/><StatCard title="Total Orders" value={stats.totalOrders || 0} description="All marketplace orders" icon={ReceiptText}/><StatCard title="Total Revenue" value={`৳${Number(stats.totalRevenue || 0).toLocaleString()}`} description="Paid payments" icon={Wallet}/></div><div className="dashboard-card"><h2 className="mb-4 text-xl font-black text-slate-950 dark:text-white">User Growth Preview</h2><SimpleLineChart data={chartData}/></div></div></RoleRoute>; }
