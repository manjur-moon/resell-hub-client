"use client";
import RoleRoute from "@/components/shared/RoleRoute";
import { getAllUsersApi, updateUserStatusApi, verifySellerApi } from "@/lib/authApi";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { BadgeCheck, Ban, CheckCircle } from "lucide-react";
import { useState } from "react";
import toast from "react-hot-toast";
export default function AdminUsersPage() { const [search, setSearch] = useState(""); const [role, setRole] = useState(""); const queryClient = useQueryClient(); const { data, isLoading } = useQuery({ queryKey: ["admin-users", search, role], queryFn: () => getAllUsersApi({ search, role, limit: 50 }) }); const users = data?.users || []; const invalidateUsers = () => queryClient.invalidateQueries({ queryKey: ["admin-users"] }); const statusMutation = useMutation({ mutationFn: updateUserStatusApi, onSuccess: data => { toast.success(data.message || "User status updated."); invalidateUsers(); }, onError: error => toast.error(error?.response?.data?.message || error.message) }); const verifyMutation = useMutation({ mutationFn: verifySellerApi, onSuccess: data => { toast.success(data.message || "Seller verification updated."); invalidateUsers(); }, onError: error => toast.error(error?.response?.data?.message || error.message) }); return <RoleRoute allowedRoles={["admin"]}><div className="space-y-6"><div><p className="text-sm font-bold uppercase tracking-[0.25em] text-orange-500">Manage Users</p><h1 className="mt-2 text-3xl font-black text-slate-950 dark:text-white">Monitor users and sellers</h1></div><div className="dashboard-card"><div className="mb-5 grid gap-3 md:grid-cols-3"><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Search user..." className="rounded-2xl border border-slate-200 bg-white px-4 py-3 dark:border-slate-700 dark:bg-slate-950"/><select value={role} onChange={e => setRole(e.target.value)} className="rounded-2xl border border-slate-200 bg-white px-4 py-3 dark:border-slate-700 dark:bg-slate-950"><option value="">All Roles</option><option value="buyer">Buyer</option><option value="seller">Seller</option><option value="admin">Admin</option></select><div className="rounded-2xl bg-slate-50 px-4 py-3 font-bold text-slate-600 dark:bg-slate-900 dark:text-slate-300">Total: {data?.total || 0}</div></div><div className="overflow-x-auto">{isLoading ? <p className="font-semibold text-slate-500">Loading users...</p> : users.length === 0 ? <p className="font-semibold text-slate-500">No users found.</p> : <table className="w-full min-w-[950px] text-left"><thead><tr className="border-b border-slate-200 text-sm text-slate-500 dark:border-slate-800"><th className="py-3">User</th><th className="py-3">Role</th><th className="py-3">Status</th><th className="py-3">Seller Verified</th><th className="py-3 text-right">Actions</th></tr></thead><tbody>{users.map(item => <tr key={item._id || item.id} className="border-b border-slate-100 dark:border-slate-800"><td className="py-4"><div className="flex items-center gap-3"><img src={item.photo || item.image || "https://i.pravatar.cc/120"} alt={item.name} className="h-12 w-12 rounded-2xl object-cover"/><div><p className="font-bold text-slate-950 dark:text-white">{item.name}</p><p className="text-sm text-slate-500">{item.email}</p></div></div></td><td className="py-4 capitalize">{item.role}</td><td className="py-4 capitalize">{item.status}</td><td className="py-4">{item.isVerifiedSeller ? "Yes" : "No"}</td><td className="py-4"><div className="flex justify-end gap-2">{item.role === "seller" && <button onClick={() => verifyMutation.mutate({ id: item._id || item.id, isVerifiedSeller: !item.isVerifiedSeller })} className="rounded-full bg-green-50 p-2 text-green-600 hover:bg-green-100 dark:bg-green-500/10"><BadgeCheck size={18}/></button>}{item.status === "active" ? <button onClick={() => statusMutation.mutate({ id: item._id || item.id, status: "blocked" })} className="rounded-full bg-red-50 p-2 text-red-500 hover:bg-red-100 dark:bg-red-500/10"><Ban size={18}/></button> : <button onClick={() => statusMutation.mutate({ id: item._id || item.id, status: "active" })} className="rounded-full bg-green-50 p-2 text-green-600 hover:bg-green-100 dark:bg-green-500/10"><CheckCircle size={18}/></button>}</div></td></tr>)}</tbody></table>}</div></div></div></RoleRoute>; }
