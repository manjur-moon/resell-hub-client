"use client";
import RoleRoute from "@/components/shared/RoleRoute"; import {useAuth} from "@/providers/AuthProvider"; export default function ProfilePage(){const {user}=useAuth(); return <RoleRoute allowedRoles={['buyer','seller','admin']}><div className="space-y-6"><div><p className="text-sm font-bold uppercase tracking-[0.25em] text-orange-500">Profile Settings</p><h1 className="mt-2 text-3xl font-black text-slate-950 dark:text-white">Manage your account</h1></div><div className="dashboard-card max-w-3xl"><div className="flex gap-5"><img src={user?.photo||user?.image||'https://i.pravatar.cc/160?img=12'} alt={user?.name||'User'} className="h-24 w-24 rounded-3xl object-cover"/><div><h2 className="text-2xl font-black">{user?.name||'User'}</h2><p>{user?.email}</p><p className="mt-2 inline-flex rounded-full bg-orange-100 px-4 py-1 text-sm font-bold capitalize text-orange-700">{user?.role}</p></div></div></div></div></RoleRoute>}
