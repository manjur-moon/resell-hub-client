"use client";
import {useAuth} from "@/providers/AuthProvider"; import {useRouter} from "next/navigation"; import {useEffect} from "react"; export default function DashboardRedirectPage(){const {user,loading}=useAuth(); const router=useRouter(); useEffect(()=>{if(loading||!user)return; if(user.role==='admin') return router.replace('/dashboard/admin'); if(user.role==='seller') return router.replace('/dashboard/seller'); router.replace('/dashboard/buyer')},[loading,user,router]); return <div className="flex min-h-[70vh] items-center justify-center">Preparing your dashboard...</div>}
