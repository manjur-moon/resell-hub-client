"use client";
import ProductForm from "@/components/products/ProductForm"; import RoleRoute from "@/components/shared/RoleRoute"; import {getProductByIdApi,updateProductApi} from "@/lib/productsApi"; import {useMutation,useQuery} from "@tanstack/react-query"; import {useParams,useRouter} from "next/navigation"; import toast from "react-hot-toast";
export default function EditProductPage(){const {id}=useParams(); const router=useRouter(); const {data,isLoading,isError}=useQuery({queryKey:['product',id],queryFn:()=>getProductByIdApi(id),enabled:Boolean(id)}); const mutation=useMutation({mutationFn:updateProductApi,onSuccess:data=>{toast.success(data.message||'Product updated successfully.'); router.push('/dashboard/seller/my-products')},onError:error=>toast.error(error?.response?.data?.message||error.message)}); return <RoleRoute allowedRoles={['seller']}><div className="space-y-6"><div><p className="text-sm font-bold uppercase tracking-[0.25em] text-orange-500">Edit Product</p><h1 className="mt-2 text-3xl font-black text-slate-950 dark:text-white">Update your listing</h1></div>{isLoading&&<div className="dashboard-card">Loading product...</div>}{isError&&<div className="dashboard-card text-red-500">Failed to load product.</div>}{data?.product&&<ProductForm initialData={data.product} onSubmit={payload=>mutation.mutate({id,payload})} loading={mutation.isPending} submitText="Update Product"/>}</div></RoleRoute>}
