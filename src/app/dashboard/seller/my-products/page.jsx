"use client";
import ConfirmModal from "@/components/shared/ConfirmModal"; import RoleRoute from "@/components/shared/RoleRoute"; import {deleteMyProductApi,getMyProductsApi} from "@/lib/productsApi"; import {useMutation,useQuery,useQueryClient} from "@tanstack/react-query"; import {Edit,Eye,Trash2} from "lucide-react"; import Link from "next/link"; import {useState} from "react"; import toast from "react-hot-toast";
function StatusBadge({status}){const styles={pending:'bg-yellow-100 text-yellow-700',available:'bg-green-100 text-green-700',rejected:'bg-red-100 text-red-700',sold:'bg-slate-100 text-slate-700'}; return <span className={`rounded-full px-3 py-1 text-xs font-bold capitalize ${styles[status]||styles.pending}`}>{status}</span>}
export default function MyProductsPage(){const qc=useQueryClient(); const [search,setSearch]=useState(''),[status,setStatus]=useState(''),[deleteId,setDeleteId]=useState(null); const {data,isLoading,isError}=useQuery({queryKey:['my-products',search,status],queryFn:()=>getMyProductsApi({search,status,limit:50})}); const products=data?.products||[]; const del=useMutation({mutationFn:deleteMyProductApi,onSuccess:data=>{toast.success(data.message||'Product deleted successfully.'); setDeleteId(null); qc.invalidateQueries({queryKey:['my-products']})},onError:e=>toast.error(e?.response?.data?.message||e.message)}); return <RoleRoute allowedRoles={['seller']}><div className="space-y-6"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-sm font-bold uppercase tracking-[0.25em] text-orange-500">My Products</p><h1 className="mt-2 text-3xl font-black">Manage your listings</h1></div><Link href="/dashboard/seller/add-product" className="btn-primary">Add Product</Link></div><div className="dashboard-card"><div className="mb-5 grid gap-3 md:grid-cols-3"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search products..." className="rounded-2xl border px-4 py-3"/><select value={status} onChange={e=>setStatus(e.target.value)} className="rounded-2xl border px-4 py-3"><option value="">All Status</option><option value="pending">Pending</option><option value="available">Available</option><option value="rejected">Rejected</option><option value="sold">Sold</option></select><div className="rounded-2xl bg-slate-50 px-4 py-3 font-bold">Total: {data?.total||0}</div></div>{isLoading&&<div>Loading products...</div>}{isError&&<div className="text-red-500">Failed to load products.</div>}{!isLoading&&!isError&&products.length===0&&<div>No products found.</div>}{products.length>0&&<div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left"><thead><tr className="border-b text-sm text-slate-500"><th className="py-3">Product</th><th>Category</th><th>Price</th><th>Stock</th><th>Status</th><th className="text-right">Actions</th></tr></thead><tbody>{products.map(p=><tr key={p._id} className="border-b"><td className="py-4"><div className="flex items-center gap-3"><img src={p.images?.[0]} alt={p.title} className="h-14 w-14 rounded-2xl object-cover"/><div><p className="font-bold">{p.title}</p><p className="text-sm text-slate-500">{p.condition}</p></div></div></td><td>{p.category}</td><td>৳{p.price}</td><td>{p.stockQuantity}</td><td><StatusBadge status={p.status}/></td><td><div className="flex justify-end gap-2"><Link href={`/products/${p._id}`}><Eye size={17}/></Link><Link href={`/dashboard/seller/edit-product/${p._id}`}><Edit size={17}/></Link><button onClick={()=>setDeleteId(p._id)} className="text-red-500"><Trash2 size={17}/></button></div></td></tr>)}</tbody></table></div>}</div><ConfirmModal isOpen={Boolean(deleteId)} title="Delete product?" message="This product will be permanently deleted." confirmText="Delete" loading={del.isPending} onClose={()=>setDeleteId(null)} onConfirm={()=>del.mutate(deleteId)}/></div></RoleRoute>}
