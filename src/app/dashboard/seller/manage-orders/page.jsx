"use client";
import RoleRoute from "@/components/shared/RoleRoute";
import { getSellerOrdersApi, updateOrderStatusApi } from "@/lib/ordersApi";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import toast from "react-hot-toast";
const orderStatuses = ["pending", "accepted", "processing", "shipped", "delivered", "cancelled", "rejected"];
export default function SellerManageOrdersPage() { const queryClient = useQueryClient(); const { data, isLoading } = useQuery({ queryKey: ["seller-orders"], queryFn: getSellerOrdersApi }); const orders = data?.orders || []; const updateMutation = useMutation({ mutationFn: updateOrderStatusApi, onSuccess: data => { toast.success(data.message || "Order updated."); queryClient.invalidateQueries({ queryKey: ["seller-orders"] }); }, onError: error => toast.error(error?.response?.data?.message || error.message) }); return <RoleRoute allowedRoles={["seller"]}><div className="space-y-6"><div><p className="text-sm font-bold uppercase tracking-[0.25em] text-orange-500">Manage Orders</p><h1 className="mt-2 text-3xl font-black text-slate-950 dark:text-white">Orders for your products</h1></div><div className="dashboard-card overflow-x-auto">{isLoading ? <p className="font-semibold text-slate-500">Loading orders...</p> : orders.length === 0 ? <p className="font-semibold text-slate-500">No orders found.</p> : <table className="w-full min-w-[900px] text-left"><thead><tr className="border-b border-slate-200 text-sm text-slate-500 dark:border-slate-800"><th className="py-3">Product</th><th className="py-3">Buyer</th><th className="py-3">Payment</th><th className="py-3">Amount</th><th className="py-3">Order Status</th></tr></thead><tbody>{orders.map(order => <tr key={order._id} className="border-b border-slate-100 dark:border-slate-800"><td className="py-4 font-bold">{order.productTitle}</td><td className="py-4"><p className="font-bold">{order.buyerInfo?.name}</p><p className="text-sm text-slate-500">{order.buyerInfo?.email}</p></td><td className="py-4 capitalize">{order.paymentStatus}</td><td className="py-4 font-bold">৳{Number(order.totalAmount).toLocaleString()}</td><td className="py-4"><select value={order.orderStatus} onChange={event => updateMutation.mutate({ id: order._id, orderStatus: event.target.value })} className="rounded-2xl border border-slate-200 bg-white px-4 py-2 text-sm dark:border-slate-700 dark:bg-slate-950">{orderStatuses.map(status => <option key={status} value={status}>{status}</option>)}</select></td></tr>)}</tbody></table>}</div></div></RoleRoute>; }
