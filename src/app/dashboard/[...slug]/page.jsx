"use client";
import ComingSoonPanel from "@/components/dashboard/ComingSoonPanel"; import RoleRoute from "@/components/shared/RoleRoute"; import {useParams} from "next/navigation"; function title(slug=[]){return slug.map(p=>p.split('-').map(w=>w.charAt(0).toUpperCase()+w.slice(1)).join(' ')).join(' / ')} export default function DashboardPlaceholderPage(){const params=useParams(); const slug=Array.isArray(params?.slug)?params.slug:[]; const role=slug[0]; const allowed=['buyer','seller','admin'].includes(role)?[role]:['buyer','seller','admin']; return <RoleRoute allowedRoles={allowed}><ComingSoonPanel title={title(slug)} description="This dashboard page route is ready. The actual feature UI and API integration will be added in upcoming phases." phase="Phase 11 or Phase 12"/></RoleRoute>}
