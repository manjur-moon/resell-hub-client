"use client";
import {getProductByIdApi} from "@/lib/productsApi"; import {useQuery} from "@tanstack/react-query"; import {useParams} from "next/navigation";
export default function ProductDetailsPage(){const {id}=useParams(); const {data,isLoading,isError}=useQuery({queryKey:['product',id],queryFn:()=>getProductByIdApi(id),enabled:Boolean(id)}); const p=data?.product; if(isLoading) return <main className="min-h-screen bg-slate-50 px-4 py-20 text-center dark:bg-slate-950">Loading product...</main>; if(isError||!p) return <main className="min-h-screen bg-slate-50 px-4 py-20 text-center dark:bg-slate-950"><p className="font-semibold text-red-500">Product could not be loaded.</p></main>; return <main className="min-h-screen bg-slate-50 px-4 py-10 dark:bg-slate-950"><div className="mx-auto max-w-5xl rounded-3xl bg-white p-6 shadow-sm dark:bg-slate-900"><img src={p.images?.[0]} alt={p.title} className="h-80 w-full rounded-3xl object-cover"/><div className="mt-6"><p className="text-sm font-bold uppercase tracking-[0.25em] text-orange-500">{p.category}</p><h1 className="mt-2 text-4xl font-black text-slate-950 dark:text-white">{p.title}</h1><p className="mt-3 text-2xl font-black text-orange-600">৳{p.price}</p><p className="mt-4 text-slate-600 dark:text-slate-300">{p.description}</p><div className="mt-6 grid gap-4 md:grid-cols-3">{[['Condition',p.condition],['Location',p.location],['Status',p.status]].map(([k,v])=><div key={k} className="rounded-2xl bg-slate-50 p-4 dark:bg-slate-950"><p className="text-sm text-slate-500">{k}</p><p className="font-bold capitalize">{v}</p></div>)}</div></div></div></main>}
