"use client";
import {uploadImageToImgbb,validateImageFile} from "@/lib/uploadImage"; import {ImagePlus,LinkIcon,Trash2,UploadCloud} from "lucide-react"; import {useRef,useState} from "react"; import toast from "react-hot-toast";
export default function ProductImageUploader({images=[],onChange,maxImages=4}){const fileRef=useRef(null); const [uploading,setUploading]=useState(false); const [manualUrl,setManualUrl]=useState(''); const handleFileChange=async(e)=>{const files=Array.from(e.target.files||[]); if(!files.length)return; if(images.length+files.length>maxImages){toast.error(`You can upload maximum ${maxImages} images.`); e.target.value=''; return;} try{files.forEach(validateImageFile); setUploading(true); const uploaded=[]; for(const file of files){const img=await uploadImageToImgbb(file); uploaded.push(img.url)} onChange([...images,...uploaded]); toast.success('Image uploaded successfully.')}catch(err){toast.error(err.message||'Image upload failed.')}finally{setUploading(false); e.target.value='';}}; const addUrl=()=>{const url=manualUrl.trim(); if(!url) return toast.error('Please enter an image URL.'); if(!url.startsWith('http')) return toast.error('Please enter a valid image URL.'); if(images.length>=maxImages) return toast.error(`You can add maximum ${maxImages} images.`); onChange([...images,url]); setManualUrl(''); toast.success('Image URL added.')}; return <div><label className="mb-2 block text-sm font-bold text-slate-700 dark:text-slate-200">Product Images</label><div className="rounded-3xl border border-dashed border-slate-300 bg-slate-50 p-5 dark:border-slate-700 dark:bg-slate-950"><input ref={fileRef} type="file" accept="image/jpeg,image/jpg,image/png,image/webp" multiple onChange={handleFileChange} className="hidden"/><div className="flex flex-col items-center justify-center rounded-2xl bg-white px-4 py-8 text-center dark:bg-slate-900"><div className="mb-4 flex h-14 w-14 items-center justify-center rounded-2xl bg-orange-100 text-orange-600"><ImagePlus size={28}/></div><h3 className="text-lg font-black text-slate-950 dark:text-white">Upload product images</h3><p className="mt-2 max-w-md text-sm text-slate-500 dark:text-slate-400">Upload JPG, PNG, or WEBP images. Maximum 3MB per image and maximum {maxImages} images.</p><button type="button" onClick={()=>fileRef.current?.click()} disabled={uploading} className="btn-primary mt-5 disabled:opacity-60"><UploadCloud size={17}/>{uploading?'Uploading...':'Choose Images'}</button></div><div className="mt-5 flex flex-col gap-3 rounded-2xl bg-white p-4 dark:bg-slate-900 sm:flex-row"><div className="flex flex-1 items-center gap-3 rounded-2xl border border-slate-200 px-4 py-3 dark:border-slate-700"><LinkIcon size={18} className="text-slate-400"/><input type="url" value={manualUrl} onChange={e=>setManualUrl(e.target.value)} placeholder="Or paste image URL manually" className="w-full bg-transparent text-sm"/></div><button type="button" onClick={addUrl} disabled={uploading} className="btn-secondary">Add URL</button></div>{images.length>0&&<div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{images.map(url=><div key={url} className="group relative overflow-hidden rounded-2xl border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900"><img src={url} alt="Product preview" className="aspect-[4/3] w-full object-cover"/><button type="button" onClick={()=>onChange(images.filter(x=>x!==url))} className="absolute right-3 top-3 rounded-full bg-red-500 p-2 text-white"><Trash2 size={16}/></button></div>)}</div>}{images.length===0&&<p className="mt-4 text-center text-sm font-semibold text-red-500">At least one product image is required.</p>}</div></div>}
