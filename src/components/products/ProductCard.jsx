"use client";
import {motion} from "framer-motion"; import {BadgeCheck,MapPin} from "lucide-react"; import Link from "next/link";
export default function ProductCard({product}){return <motion.article initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true}} className="group flex h-full flex-col overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl dark:border-slate-800 dark:bg-slate-900"><div className="relative aspect-[4/3] overflow-hidden bg-slate-100 dark:bg-slate-800"><img src={product.images?.[0]||'https://images.unsplash.com/photo-1553531889-56cc480ac5cb?q=80&w=1200&auto=format&fit=crop'} alt={product.title} className="h-full w-full object-cover transition duration-500 group-hover:scale-105"/><div className="absolute left-4 top-4 rounded-full bg-white/90 px-3 py-1 text-xs font-black text-slate-800 backdrop-blur">{product.condition}</div>{product.sellerInfo?.isVerifiedSeller&&<div className="absolute right-4 top-4 flex items-center gap-1 rounded-full bg-green-500 px-3 py-1 text-xs font-black text-white"><BadgeCheck size={14}/>Verified</div>}</div><div className="flex flex-1 flex-col p-5"><div className="mb-3 flex items-center justify-between gap-3"><span className="rounded-full bg-orange-100 px-3 py-1 text-xs font-black text-orange-700">{product.category}</span><span className="text-lg font-black text-orange-600">৳{Number(product.price).toLocaleString()}</span></div><h3 className="line-clamp-2 text-xl font-black text-slate-950 dark:text-white">{product.title}</h3><p className="mt-3 line-clamp-2 flex-1 text-sm text-slate-600 dark:text-slate-300">{product.description}</p><div className="mt-4 flex items-center gap-2 text-sm font-semibold text-slate-500"><MapPin size={16}/><span className="line-clamp-1">{product.location}</span></div><div className="mt-5 flex items-center justify-between gap-3"><div><p className="text-xs text-slate-500">Seller</p><p className="line-clamp-1 text-sm font-bold text-slate-800 dark:text-slate-100">{product.sellerInfo?.name||'Unknown Seller'}</p></div><Link href={`/products/${product._id}`} className="rounded-full bg-slate-950 px-5 py-2.5 text-sm font-bold text-white transition hover:bg-orange-600">View Details</Link></div></div></motion.article>}
