"use client";
import SkeletonCard from "@/components/shared/SkeletonCard"; import {getProductCategoriesApi,getProductsApi} from "@/lib/productsApi"; import {useQuery} from "@tanstack/react-query"; import {PackageSearch} from "lucide-react"; import {useRouter,useSearchParams} from "next/navigation"; import {useMemo} from "react"; import Pagination from "./Pagination"; import ProductCard from "./ProductCard"; import ProductFilters from "./ProductFilters";
function clean(obj){const c={}; Object.entries(obj).forEach(([k,v])=>{if(v!==undefined&&v!==null&&v!=='') c[k]=v}); return c;}
export default function ProductsClient(){const router=useRouter(); const sp=useSearchParams(); const query=useMemo(()=>({search:sp.get('search')||'',category:sp.get('category')||'',condition:sp.get('condition')||'',minPrice:sp.get('minPrice')||'',maxPrice:sp.get('maxPrice')||'',location:sp.get('location')||'',sort:sp.get('sort')||'newest',page:sp.get('page')||'1',limit:'12'}),[sp]); const updateUrl=next=>{const params=new URLSearchParams(); Object.entries(clean(next)).forEach(([k,v])=>params.set(k,v)); router.push(`/products?${params.toString()}`)}; const {data,isLoading,isError}=useQuery({queryKey:['products',query],queryFn:()=>getProductsApi(query)}); const {data:cat}=useQuery({queryKey:['product-categories'],queryFn:getProductCategoriesApi}); const products=data?.products||[], total=data?.total||0, totalPages=data?.totalPages||0, page=Number(data?.page||query.page||1); return <main className="min-h-screen bg-slate-50 px-4 py-10 dark:bg-slate-950"><section className="mx-auto max-w-7xl"><div className="mb-8 rounded-[2rem] bg-slate-950 p-8 text-white md:p-12"><p className="text-sm font-bold uppercase tracking-[0.3em] text-orange-400">ReSell Hub Marketplace</p><div className="mt-4 grid gap-6 lg:grid-cols-[1.4fr_0.6fr] lg:items-end"><div><h1 className="text-4xl font-black md:text-6xl">Find affordable pre-owned products</h1><p className="mt-4 max-w-2xl text-slate-300">Search, filter, and compare second-hand products from trusted sellers around Bangladesh.</p></div><div className="rounded-3xl bg-white/10 p-5 backdrop-blur"><p className="text-sm text-slate-300">Available Products</p><p className="mt-2 text-4xl font-black text-orange-400">{total}</p></div></div></div><ProductFilters initialFilters={query} categories={cat?.categories||[]} onApply={f=>updateUrl({...f,page:'1',limit:'12'})} onReset={()=>router.push('/products')}/><div className="mt-8"><h2 className="text-2xl font-black text-slate-950 dark:text-white">All Products</h2><p className="mt-1 text-sm text-slate-500 dark:text-slate-400">Showing {products.length} of {total} products</p></div>{isLoading&&<div className="mt-8 grid gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{Array.from({length:8}).map((_,i)=><SkeletonCard key={i}/>)}</div>}{isError&&<div className="mt-8 rounded-3xl border border-red-100 bg-red-50 p-8 text-center"><h3 className="text-2xl font-black text-red-600">Products could not be loaded</h3></div>}{!isLoading&&!isError&&products.length===0&&<div className="mt-8 rounded-3xl border border-slate-200 bg-white p-10 text-center dark:border-slate-800 dark:bg-slate-900"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-3xl bg-orange-100 text-orange-600"><PackageSearch size={30}/></div><h3 className="mt-5 text-2xl font-black text-slate-950 dark:text-white">No products found</h3><p className="mt-2 text-slate-500">Try changing your search, filters, or price range.</p></div>}{!isLoading&&!isError&&products.length>0&&<><div className="mt-8 grid items-stretch gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{products.map(p=><ProductCard key={p._id} product={p}/>)}</div><Pagination page={page} totalPages={totalPages} onPageChange={n=>updateUrl({...query,page:String(n),limit:'12'})}/></>}</section></main>}
