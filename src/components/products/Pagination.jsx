"use client";
export default function Pagination({page=1,totalPages=1,onPageChange}){if(!totalPages||totalPages<=1)return null; const current=Number(page)||1; const pages=Array.from({length:totalPages},(_,i)=>i+1); return <div className="mt-10 flex flex-wrap items-center justify-center gap-2"><button onClick={()=>onPageChange(current-1)} disabled={current<=1} className="btn-secondary disabled:opacity-50">Previous</button>{pages.slice(0,7).map(n=><button key={n} onClick={()=>onPageChange(n)} className={`inline-flex h-11 w-11 items-center justify-center rounded-full text-sm font-black transition ${n===current?'bg-orange-500 text-white':'border border-slate-200 bg-white text-slate-700 hover:text-orange-650 dark:border-slate-700 dark:bg-slate-900 dark:text-slate-200'}`}>{n}</button>)}<button onClick={()=>onPageChange(current+1)} disabled={current>=totalPages} className="btn-secondary disabled:opacity-50">Next</button></div>}
